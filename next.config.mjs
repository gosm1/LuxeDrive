/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['eu-central-1-shared-euc1-02.graphassets.com', 'media.graphassets.com']
    }
};

export default nextConfig;
